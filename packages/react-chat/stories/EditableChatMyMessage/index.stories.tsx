import { Meta } from '@storybook/react';
import { EditableChatMyMessage } from '@fluentui-contrib/react-chat';
export { Default } from './Default.stories';

const meta: Meta<typeof EditableChatMyMessage> = {
  component: EditableChatMyMessage,
};

export default meta;
